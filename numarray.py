#A numarray is a coolection of elements of the same data type stored in a multidimensional structure
#Arrays provide an efficient way of storing and performing operations of numeric data.
#Elements are accessed using square brackets []
#Arrays are commonly created from python lists.

#Create a numpy Array
#Arrays are created using the np.array() function.

#One-dimensional array from a list
import numpy as np
#array1 = np.array([100, 200, 300, 400, 500])
#print(array1)

#Two-dimensional array from a list of lists

array2 = np.array([[10, 20, 30], [40, 50, 60]])
print(array2)

#one-dimensional array from a tuple
#array3 = np.array((1, 2, 3, 4,))
#print(array3)